#include "main.h"
/**
 * add - function that adds two integers and returns the result
 *
 * Return: 0 Always
 *
 * @a: parameter to be summed
 *
 * @b: parameter to be summed
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
