#include "main.h"
/**
 * _abs - function that computers the absolute value of an integer
 *
 * Return: 0 Always
 *
 * @n: Parameter to be printed
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
		n = -(n);
	{
		return (n);
	}
}
